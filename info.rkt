
#lang info

(define collection "cirru–parser")
(define blurb
  list
    `(p "Cirru Parser in Racket"))
(define categories '(metaprogramming))
(define homepage "https://github.com/Cirru/parser-rkt")
(define version "0.1")
